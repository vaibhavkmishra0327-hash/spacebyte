
  # Build SpaceByte Platform

  This is a code bundle for Build SpaceByte Platform. The original project is available at https://www.figma.com/design/Biia63ejyhveclUtEhBNzi/Build-SpaceByte-Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  